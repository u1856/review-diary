<% if @movie.liked_by?(current_user) %>
  <%= link_to "", movie_likes_path(@movie), method: :delete, remote: true, class: "fas fa-bookmark" %></td>
<% else %>
  <%= link_to "", movie_likes_path(@movie), method: :post, remote: true, class: "far fa-bookmark" %></td>
<% end %>